interface:
  display_name: "Project Memory"
  short_description: "Repo-specific patterns and workflows for project-memory"
  default_prompt: "Use the project-memory repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true